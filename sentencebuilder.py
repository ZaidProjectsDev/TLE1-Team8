import numpy as np

detections_str = ""
def sentencebuilder(detections):
   #detections_str = np.array_str(detections)
   print(f'''The current scene contains {detections}''')
   return detections_str